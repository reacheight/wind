POST        /analysis/:matchId                  controllers.AnalysisController.postAnalysis(matchId: Long)

GET         /analysis/:matchId/state            controllers.AnalysisController.getAnalysisState(matchId: Long)

GET         /analysis/count                     controllers.AnalysisController.getAnalysisCount

GET         /analysis/:matchId                  controllers.AnalysisController.getAnalysis(matchId: Long)

GET         /icons/:heroId                      controllers.IconsController.getIcon(heroId: Int)

GET         /miniportraits/:heroId              controllers.IconsController.getMiniPortrait(heroId: Int)

GET         /items/:itemId                      controllers.IconsController.getItemIcon(itemId: Int)

GET         /images/abilities/:abilityId        controllers.IconsController.getAbilityIcon(abilityId: Int)

GET         /login                              controllers.AccountController.login

GET         /logout                             controllers.AccountController.logout

GET         /login-callback                     controllers.AccountController.loginCallback

GET         /user                               controllers.AccountController.getUser

GET         /matches/:matchId                   controllers.MatchesController.getMatch(matchId: Long)

GET         /players/:accountId/matches         controllers.MatchesController.getMatches(accountId: Long)